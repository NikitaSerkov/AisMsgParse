services:
  ais-monitor:
    build:
      context: https://github.com/NikitaSerkov/ais-monitor.git #main
    container_name: ais-monitor
    volumes:
      - ./conf.ini:/root/conf.ini
      - ./ais-archive:/root/Archive
    restart: unless-stopped


